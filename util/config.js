const idbName = 'idb';
const idbFilename = `${idbName}/.idb`;

const tableName = 'table-name';
const tableFilename = `${idbName}/${tableName}.table`;

module.exports = {
  idbFilename,
  idbName,
  tableFilename,
  tableName
};
